export enum Route {
  Home = "/home",
  SignIn = "/signin",
  Account = "/account",
  Skills = "/skills",
  Team = "/team",
  Search = "/search",
  Settings = "/settings",
}

export enum Role {
  Guest,
  Employee,
  Manager,
  Admin,
  Superadmin,
}
